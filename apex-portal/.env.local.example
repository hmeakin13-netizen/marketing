# Supabase project settings — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Notion internal integration token ("Apex Portal") — server-only, never exposed to the browser
NOTION_API_KEY=
